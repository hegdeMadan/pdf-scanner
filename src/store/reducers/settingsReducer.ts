import { settings } from './initialState/settingsState';

export const settingsReducer = (state = settings) => {
  switch (key) {
    case value:
      
      break;
  
    default:
      break;
  }
}